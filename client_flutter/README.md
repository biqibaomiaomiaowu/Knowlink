# knowlink_client

KnowLink MVP Flutter client.

## Local Development

Use Flutter `3.41.5` so local analysis matches GitHub Actions.

Recommended checks:

```powershell
flutter --version
flutter pub get
flutter analyze
flutter test
```
